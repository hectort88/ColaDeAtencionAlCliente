# ColaDeAtencionAlCliente

Dependencias:
* dotnet core 3.1
* SignalR
* EntityFramework
* Utf8Json
* Vue.js

Utiliza sql server localdb y vue.js para manipulacion de estado en el front

Ejecutar la migracion con:
`update-database`
